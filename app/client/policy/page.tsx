import React from 'react';
import Policy from '../components/info-pages/policy'; // Adjusted the import path

const PolicyPage: React.FC = () => {
    return (
        <Policy />
    );
};

export default PolicyPage;
